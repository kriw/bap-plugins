#include <stdio.h>

int main(int argc) {
    int po = (argc + 3) * 11;
    if (po > 2) {
        printf("(argc + 3) * 11: %d\n", po);
    } else {
        printf("atheonhuant\n");
    }
    return 0;
}
